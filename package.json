{
  "name": "comeon-pos",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build step' && exit 0"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  }
}
